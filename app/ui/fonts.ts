import { Nunito, Source_Serif_4 } from 'next/font/google';

export const nunito = Nunito({ weight: 'variable', subsets: ['latin'] });

export const sourceSerif = Source_Serif_4({ weight: 'variable', subsets: ['latin'] });
